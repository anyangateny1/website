const config = {
    // Replace this URL with your actual API Gateway URL
    apiBaseUrl: 'https://yhrh5asyof.execute-api.ap-southeast-2.amazonaws.com/prod',
    endpoints: {
        projects: '/api/projects',
        contact: '/api/contact',
        images: '/api/images'
    }
};

export default config; 